apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.labels.app }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }} #svc port number and its our wish
      targetPort: {{ .Values.port}} # container port number and we shouldn't change it
  type: {{ .Values.service.type }}

vaules.yaml

namespace: murali-ns
replicas: 2
labels:
  app: ipl
name: c-1
image:
  repository: muralisocial123/ipl
  tag: latest
port: 3000
service:
  port: 80
  type: LoadBalancer